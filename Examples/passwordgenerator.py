from random import randint

def passgen(str, num, extra):
    password = ""
    randnum = randint(1, 3)
    if randint == 1:
        for i in range(0, len(str)):
            print (i)
    if randint == 2:
        for i in range(0, len(str)):
            print (i)
    if randint == 3:
        for i in range(0, len(str)):
            print (i)